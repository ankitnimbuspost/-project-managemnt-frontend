import React, { useEffect } from "react";
import { useNavigate } from "react-router-dom";

export default function Logout() {
    // let navigatev = useNavigate(null);
    // useEffect(() => {
    //     console.log("Logout page.")
    //     navigatev("/signin");
    // },[])
    return <>
    <h1>Logout Page</h1>
    </>;
}